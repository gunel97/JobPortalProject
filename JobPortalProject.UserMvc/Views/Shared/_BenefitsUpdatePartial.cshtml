@model JobUpdateViewModel

    <div class="col-lg-12">
        <div class="section-title2">
            <h5 id="benefitTitle"></h5>
        </div>
    </div>
    <div id="bContainer" class="col-md-12">
        <div class="form-inner mb-25">

            <div class="col-lg-12">
                <div class="section-title2">
                    <h5>Extra Benefits :</h5>
                </div>
            </div>

            @for (var b = 0; b < Model.ExtraBenefits.Count(); b++)
            {
                <input asp-for="ExtraBenefits[b].Id" value="@Model.ExtraBenefits[b].Id" type="hidden" />
                @for (int i = 0; i < Model.ExtraBenefits[b].JobExtraBenefitTranslations.Count(); i++)
                {
                    <input asp-for="ExtraBenefits[b].JobExtraBenefitTranslations[i].Id"
                           value="@Model.ExtraBenefits[b].JobExtraBenefitTranslations[i].Id" type="hidden" />
                    <label for="jobtitle">
                         <img src="@Model.JobTranslations[i].LanguageIcon" alt=""> 
                        Benefit @(b + 1)
                    </label>
                    <div class="input-area">
                        <img src="/images/icon/company-2.svg" alt="">
                        <input asp-for="ExtraBenefits[b].JobExtraBenefitTranslations[i].Value" placeholder="Senior UI/UX Engineer">
                        <input type="hidden" asp-for="ExtraBenefits[b].JobExtraBenefitTranslations[i].LanguageId"
                               value="@Model.ExtraBenefits[b].JobExtraBenefitTranslations[i].LanguageId" />
                    </div>
                }
                <div class="add-row">
                    <button type="button"
                            onclick="deleteJobBenefit(@Model.ExtraBenefits[b].Id)" class="remove-work-area-row remove">
                        Remove
                    </button>
                </div>
            }
        </div>
    </div>
    <br />
